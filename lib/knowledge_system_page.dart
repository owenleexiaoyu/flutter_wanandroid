import 'package:flutter/material.dart';

class KnowledgeSystemPage extends StatelessWidget {
  const KnowledgeSystemPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("KnowledgeSystemPage"),
      ),
      body: Container(),
    );
  }
}
