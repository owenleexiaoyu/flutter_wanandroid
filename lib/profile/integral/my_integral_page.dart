import 'package:flutter/material.dart';

class MyIntegralPage extends StatelessWidget {
  const MyIntegralPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("MyIntegralPage"),
      ),
      body: Container(),
    );
  }
}
